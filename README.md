# install scripts (Moved to dotfiles repo)
This repository contains dotfiles (usually configurations) for different Unix Desktop Environment

Currently the fedora_install_script file is updated for Fedora 31

